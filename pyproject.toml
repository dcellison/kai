[project]
name = "kai"
version = "0.1.0"
description = "Telegram gateway to Claude Code CLI"
license = "Apache-2.0"
requires-python = ">=3.9"
dependencies = [
    "python-telegram-bot[ext]>=20.0",
    "aiosqlite>=0.19.0",
    "python-dotenv>=1.0.0",
    "aiohttp>=3.9.0",
]
